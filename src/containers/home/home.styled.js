import styled from "styled-components";

export const StyledHomeContainer = styled.div`
  padding: 24px;
  height: calc(100% - 48px);
  display: flex;
  gap: 20px;
  flex-wrap: wrap;
  overflow-y: scroll;
`;
